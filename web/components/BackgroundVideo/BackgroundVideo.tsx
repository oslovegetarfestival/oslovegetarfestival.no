import { Section } from "components/Layout"
import { Video } from "components/Video"

export const BackgroundVideo = () => {
  return (
    <Section width="large" noPadding="sides">
      <Video id="rUthsu7i7v8" isAutoplay />
    </Section>
  )
}
